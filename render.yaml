services:
  - type: web
    name: dad-magic-socket-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    rootDir: deploy
    envVars:
      - key: NODE_ENV
        value: production
