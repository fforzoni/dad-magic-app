services:
  - type: web
    name: dad-magic-socket-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run ultra
    rootDir: render-server
    envVars:
      - key: NODE_ENV
        value: production
